{
  "buildCommand": "npx pnpm install && npx pnpm vite build",
  "outputDirectory": "dist/public",
  "installCommand": "npx pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/hub", "destination": "/hub.html" }
  ]
}
